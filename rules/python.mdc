---
description: Python-specific conventions and tooling standards
globs: "**/*.py"
---

# Python Standards

## Project Structure
- Tests in `tests/` directory
- Dependencies in `requirements.txt` or `pyproject.toml`
- Use virtual environments (`pyenv activate <env_name>` or `workon <env_name>`)

## Code Style
- Black formatting (88 char line length)
- PEP 8 naming: `snake_case` functions/variables, `PascalCase` classes, `UPPER_CASE` constants
- Absolute imports over relative

## Type Hints
- Type all function parameters and return values
- Use `Optional[Type]` for nullable types
- Use `Protocol` for duck typing

## Documentation
- Google-style docstrings for public APIs

## Logging & Errors
- Use loguru for structured logging — never print() for application output
- Specific try-except blocks (no bare except)
- Clear, actionable error messages

## Dependencies
- Pin versions in requirements.txt
